# Behavioural checks of the linked userspace images (issue #61)

Run on the Tiger G5 after `Tools/userspace/link_corpus.py` + `build.sh` (see `Userspace/README.md`, "Linking"). Build with `gcc -arch ppc -w -std=gnu99 X.c [-framework OpenGL]`.
Always give the rebuilt image a *different install name* (`INSTALL=/path/x.dylib sh build.sh out`): `dlopen` of a copy that keeps the stock name returns the already-loaded system image.

| test | what it does | result (2026-09-19) |
|---|---|---|
| `libgl_test2.c` | OpenGL smoke test (offscreen CGL context, clear, quad, readback, thread tag path). Run normally and with `DYLD_LIBRARY_PATH` pointing at a directory holding the rebuilt `libGL.dylib` (stock install name), so the whole process, framework included, uses it | identical output; `dladdr` shows glClear inside the rebuilt image |
| `ga_test.c` | stock and rebuilt GA plug-in side by side: exports, `window_mode_to_ati_format` over 2.16M inputs, `ati_format_info_table` (48 pointer words compared by target) | PASS, 0 differences |
| `va_test.c` | stock and rebuilt VA bundle: exports, `AVAGetRendererInfo` return + buffer | PASS (2026-09-20: 63/63 exports; `FUN_00115fa0` is real code now) |
| `glprog_test.c` | stock and rebuilt libGLProgrammability loaded side by side: all 515 stock C exports resolve; ARB vertex/fragment program parser (create, attach stream, attach string, parse; status + stream size + every stream word) over 39 programs x 2 shader kinds | PASS: 78 parses, 0 differences (2026-09-20, call-accurate corpus) |
| `glprog_parsestatement_probe.c`, `glprog_strtod_probe.c` | single-function probes used to bisect the first failures (statement kinds; `glp_strtod`) | historical: the failures they found are the rewrites listed in `Userspace/README.md` |
| `gld_test.c` | stock and rebuilt GL driver bundle side by side (static initialisers run at dlopen); all 63 stock exports resolve in both | PASS (2026-09-20: 63/63 exports; `FUN_00115fa0` is real code now) |
| `glsl_probe.c` (+ `glsl_libm.vert`) | one image, GLSL entry points in order: `ShInitialize` (builds the built-in symbol table by parsing the whole built-in GLSL source), `ShConstructCompiler`, `ShCompile` of a vertex shader (built-in one-liner, or the file given as 5th argument), info log, program string (`glsl_probe IMAGE MODE DEBUG STAGE [FILE]`) | 2026-09-25: the rebuilt image prints the same ARB program as the stock for both shaders (only the two heap addresses the probe prints differ) |
| `glsl_test.c` + `glsl_gen_shaders.py` | stock vs rebuilt GLSL front end over 141 hand-written shaders (valid and error cases), each image in its own forked child, three ShCompile variants (parse only, tree dump + code gen, code gen with the PP stream words) | not yet runnable end to end on the rebuilt image (blocked by the probe above) |
| `atomic_test.c` | two threads hammering the pool-string refcount path (the `lwarx/stwcx.` loops rewritten as CAS loops) in the stock and in the rebuilt libGLProgrammability | PASS on both, no lost updates (#69) |
| `eh_test.cpp` | a C++ exception thrown by the image's own `std::string::_S_create` into this program: caught as `length_error`, and the unmatched-type case | stock and rebuilt both end in SIGABRT (rc 134): stock via `terminate`, rebuilt in `uw_init_context_1` - no FDEs for the rebuilt code (#63); before the unwinder rebuild the rebuilt image died with SIGBUS |
| `ftoa_test.c` | GLDriver's float formatter FUN_000cdc3c (ecvt-based, not exported: nm offset rebased on an export) on 20 values, stock vs rebuilt | identical (2026-09-25); the build before Stage B3 step 12 died with SIGBUS, an intermediate one printed every exponent as `e00` |
| `glsl_intfold.vert`, `glsl_intfold2.vert` (with `glsl_probe.c`) | scalar and vector integer constant folding in the GLSL front end | identical ARB programs (2026-09-25); before Stage B3 step 15 the rebuilt image folded the vector cases to 0 / garbage and reported a divide by zero |
